<!--This Document is generated by GameMaker, if you edit it by hand then you do so at your own risk!-->
<timeline>
  <entry>
    <step>60</step>
    <event>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>with (objTrailblazer)
{
    //Drop a trail
    instance_create(x,y,objTrail);
    //Find direction
    var east, north, west, south;
    var direct = -1;
    //while (direct == -1)
    {
        if (place_meeting(x + 8,y,objTrailmap)) east = 0;
        else east = -1;
        if (place_meeting(x,y - 8,objTrailmap)) north = 1;
        else north = -1;
        if (place_meeting(x - 8,y,objTrailmap)) west = 2;
        else west = -1;
        if (place_meeting(x,y + 8,objTrailmap)) south = 3;
        else south = -1;
        
        while (direct == -1)
        {
            direct = choose(east,north,west,south);
        }
        /*
        if (direct != -1) break;
        if (east != -1) direct = east; break;
        if (north != -1) direct = north; break;
        if (west != -1) direct = west; break;
        if (south != -1) direct = south; break; */
    }
    //Move in that direction
    switch (direct)
    {
        case 0: x += 8; break; //East
        case 1: y -= 8; break; //North
        case 2: x -= 8; break; //West
        case 3: y += 8; break; //South
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </entry>
  <entry>
    <step>90</step>
    <event>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>with (objTrailblazer)
{
    //Drop a trail
    instance_create(x,y,objTrail);
    //Find direction
    var east, north, west, south;
    var direct = -1;
    //while (direct == -1)
    {
        if (place_meeting(x + 8,y,objTrailmap)) east = 0;
        else east = -1;
        if (place_meeting(x,y - 8,objTrailmap)) north = 1;
        else north = -1;
        if (place_meeting(x - 8,y,objTrailmap)) west = 2;
        else west = -1;
        if (place_meeting(x,y + 8,objTrailmap)) south = 3;
        else south = -1;
        
        while (direct == -1)
        {
            direct = choose(east,north,west,south);
        }
        /*
        if (direct != -1) break;
        if (east != -1) direct = east; break;
        if (north != -1) direct = north; break;
        if (west != -1) direct = west; break;
        if (south != -1) direct = south; break; */
    }
    //Move in that direction
    switch (direct)
    {
        case 0: x += 8; break; //East
        case 1: y -= 8; break; //North
        case 2: x -= 8; break; //West
        case 3: y += 8; break; //South
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </entry>
  <entry>
    <step>120</step>
    <event>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>with (objTrailblazer)
{
    //Drop a trail
    instance_create(x,y,objTrail);
    //Find direction
    var east, north, west, south;
    var direct = -1;
    //while (direct == -1)
    {
        if (place_meeting(x + 8,y,objTrailmap)) east = 0;
        else east = -1;
        if (place_meeting(x,y - 8,objTrailmap)) north = 1;
        else north = -1;
        if (place_meeting(x - 8,y,objTrailmap)) west = 2;
        else west = -1;
        if (place_meeting(x,y + 8,objTrailmap)) south = 3;
        else south = -1;
        
        while (direct == -1)
        {
            direct = choose(east,north,west,south);
        }
        /*
        if (direct != -1) break;
        if (east != -1) direct = east; break;
        if (north != -1) direct = north; break;
        if (west != -1) direct = west; break;
        if (south != -1) direct = south; break; */
    }
    //Move in that direction
    switch (direct)
    {
        case 0: x += 8; break; //East
        case 1: y -= 8; break; //North
        case 2: x -= 8; break; //West
        case 3: y += 8; break; //South
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </entry>
  <entry>
    <step>150</step>
    <event>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>with (objTrailblazer)
{
    //Drop a trail
    instance_create(x,y,objTrail);
    //Find direction
    var east, north, west, south;
    var direct = -1;
    //while (direct == -1)
    {
        if (place_meeting(x + 8,y,objTrailmap)) east = 0;
        else east = -1;
        if (place_meeting(x,y - 8,objTrailmap)) north = 1;
        else north = -1;
        if (place_meeting(x - 8,y,objTrailmap)) west = 2;
        else west = -1;
        if (place_meeting(x,y + 8,objTrailmap)) south = 3;
        else south = -1;
        
        while (direct == -1)
        {
            direct = choose(east,north,west,south);
        }
        /*
        if (direct != -1) break;
        if (east != -1) direct = east; break;
        if (north != -1) direct = north; break;
        if (west != -1) direct = west; break;
        if (south != -1) direct = south; break; */
    }
    //Move in that direction
    switch (direct)
    {
        case 0: x += 8; break; //East
        case 1: y -= 8; break; //North
        case 2: x -= 8; break; //West
        case 3: y += 8; break; //South
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </entry>
  <entry>
    <step>180</step>
    <event>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>with (objTrailblazer)
{
    //Drop a trail
    instance_create(x,y,objTrail);
    //Find direction
    var east, north, west, south;
    var direct = -1;
    //while (direct == -1)
    {
        if (place_meeting(x + 8,y,objTrailmap)) east = 0;
        else east = -1;
        if (place_meeting(x,y - 8,objTrailmap)) north = 1;
        else north = -1;
        if (place_meeting(x - 8,y,objTrailmap)) west = 2;
        else west = -1;
        if (place_meeting(x,y + 8,objTrailmap)) south = 3;
        else south = -1;
        
        while (direct == -1)
        {
            direct = choose(east,north,west,south);
        }
        /*
        if (direct != -1) break;
        if (east != -1) direct = east; break;
        if (north != -1) direct = north; break;
        if (west != -1) direct = west; break;
        if (south != -1) direct = south; break; */
    }
    //Move in that direction
    switch (direct)
    {
        case 0: x += 8; break; //East
        case 1: y -= 8; break; //North
        case 2: x -= 8; break; //West
        case 3: y += 8; break; //South
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </entry>
  <entry>
    <step>210</step>
    <event>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>with (objTrailblazer)
{
    //Drop a trail
    instance_create(x,y,objTrail);
    //Find direction
    var east, north, west, south;
    var direct = -1;
    //while (direct == -1)
    {
        if (place_meeting(x + 8,y,objTrailmap)) east = 0;
        else east = -1;
        if (place_meeting(x,y - 8,objTrailmap)) north = 1;
        else north = -1;
        if (place_meeting(x - 8,y,objTrailmap)) west = 2;
        else west = -1;
        if (place_meeting(x,y + 8,objTrailmap)) south = 3;
        else south = -1;
        
        while (direct == -1)
        {
            direct = choose(east,north,west,south);
        }
        /*
        if (direct != -1) break;
        if (east != -1) direct = east; break;
        if (north != -1) direct = north; break;
        if (west != -1) direct = west; break;
        if (south != -1) direct = south; break; */
    }
    //Move in that direction
    switch (direct)
    {
        case 0: x += 8; break; //East
        case 1: y -= 8; break; //North
        case 2: x -= 8; break; //West
        case 3: y += 8; break; //South
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </entry>
  <entry>
    <step>240</step>
    <event>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>with (objTrailblazer)
{
    //Drop a trail
    instance_create(x,y,objTrail);
    //Find direction
    var east, north, west, south;
    var direct = -1;
    //while (direct == -1)
    {
        if (place_meeting(x + 8,y,objTrailmap)) east = 0;
        else east = -1;
        if (place_meeting(x,y - 8,objTrailmap)) north = 1;
        else north = -1;
        if (place_meeting(x - 8,y,objTrailmap)) west = 2;
        else west = -1;
        if (place_meeting(x,y + 8,objTrailmap)) south = 3;
        else south = -1;
        
        while (direct == -1)
        {
            direct = choose(east,north,west,south);
        }
        /*
        if (direct != -1) break;
        if (east != -1) direct = east; break;
        if (north != -1) direct = north; break;
        if (west != -1) direct = west; break;
        if (south != -1) direct = south; break; */
    }
    //Move in that direction
    switch (direct)
    {
        case 0: x += 8; break; //East
        case 1: y -= 8; break; //North
        case 2: x -= 8; break; //West
        case 3: y += 8; break; //South
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </entry>
  <entry>
    <step>270</step>
    <event>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>with (objTrailblazer)
{
    //Drop a trail
    instance_create(x,y,objTrail);
    //Find direction
    var east, north, west, south;
    var direct = -1;
    //while (direct == -1)
    {
        if (place_meeting(x + 8,y,objTrailmap)) east = 0;
        else east = -1;
        if (place_meeting(x,y - 8,objTrailmap)) north = 1;
        else north = -1;
        if (place_meeting(x - 8,y,objTrailmap)) west = 2;
        else west = -1;
        if (place_meeting(x,y + 8,objTrailmap)) south = 3;
        else south = -1;
        
        while (direct == -1)
        {
            direct = choose(east,north,west,south);
        }
        /*
        if (direct != -1) break;
        if (east != -1) direct = east; break;
        if (north != -1) direct = north; break;
        if (west != -1) direct = west; break;
        if (south != -1) direct = south; break; */
    }
    //Move in that direction
    switch (direct)
    {
        case 0: x += 8; break; //East
        case 1: y -= 8; break; //North
        case 2: x -= 8; break; //West
        case 3: y += 8; break; //South
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </entry>
  <entry>
    <step>300</step>
    <event>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>with (objTrailblazer)
{
    //Drop a trail
    instance_create(x,y,objTrail);
    //Find direction
    var east, north, west, south;
    var direct = -1;
    //while (direct == -1)
    {
        if (place_meeting(x + 8,y,objTrailmap)) east = 0;
        else east = -1;
        if (place_meeting(x,y - 8,objTrailmap)) north = 1;
        else north = -1;
        if (place_meeting(x - 8,y,objTrailmap)) west = 2;
        else west = -1;
        if (place_meeting(x,y + 8,objTrailmap)) south = 3;
        else south = -1;
        
        while (direct == -1)
        {
            direct = choose(east,north,west,south);
        }
        /*
        if (direct != -1) break;
        if (east != -1) direct = east; break;
        if (north != -1) direct = north; break;
        if (west != -1) direct = west; break;
        if (south != -1) direct = south; break; */
    }
    //Move in that direction
    switch (direct)
    {
        case 0: x += 8; break; //East
        case 1: y -= 8; break; //North
        case 2: x -= 8; break; //West
        case 3: y += 8; break; //South
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </entry>
  <entry>
    <step>330</step>
    <event>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>with (objTrailblazer)
{
    //Drop a trail
    instance_create(x,y,objTrail);
    //Find direction
    var east, north, west, south;
    var direct = -1;
    //while (direct == -1)
    {
        if (place_meeting(x + 8,y,objTrailmap)) east = 0;
        else east = -1;
        if (place_meeting(x,y - 8,objTrailmap)) north = 1;
        else north = -1;
        if (place_meeting(x - 8,y,objTrailmap)) west = 2;
        else west = -1;
        if (place_meeting(x,y + 8,objTrailmap)) south = 3;
        else south = -1;
        
        while (direct == -1)
        {
            direct = choose(east,north,west,south);
        }
        /*
        if (direct != -1) break;
        if (east != -1) direct = east; break;
        if (north != -1) direct = north; break;
        if (west != -1) direct = west; break;
        if (south != -1) direct = south; break; */
    }
    //Move in that direction
    switch (direct)
    {
        case 0: x += 8; break; //East
        case 1: y -= 8; break; //North
        case 2: x -= 8; break; //West
        case 3: y += 8; break; //South
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </entry>
  <entry>
    <step>360</step>
    <event>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>with (objTrailblazer)
{
    //Drop a trail
    instance_create(x,y,objTrail);
    //Find direction
    var east, north, west, south;
    var direct = -1;
    //while (direct == -1)
    {
        if (place_meeting(x + 8,y,objTrailmap)) east = 0;
        else east = -1;
        if (place_meeting(x,y - 8,objTrailmap)) north = 1;
        else north = -1;
        if (place_meeting(x - 8,y,objTrailmap)) west = 2;
        else west = -1;
        if (place_meeting(x,y + 8,objTrailmap)) south = 3;
        else south = -1;
        
        while (direct == -1)
        {
            direct = choose(east,north,west,south);
        }
        /*
        if (direct != -1) break;
        if (east != -1) direct = east; break;
        if (north != -1) direct = north; break;
        if (west != -1) direct = west; break;
        if (south != -1) direct = south; break; */
    }
    //Move in that direction
    switch (direct)
    {
        case 0: x += 8; break; //East
        case 1: y -= 8; break; //North
        case 2: x -= 8; break; //West
        case 3: y += 8; break; //South
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </entry>
</timeline>
